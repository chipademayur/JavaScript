// for in loop is use for iterate overall enamerable of an object or to walk overall keys of an object for in loop is use
let student = {
  Mayuur: 90,
  ritesh: 95,
  vishu: 100,
  prajwel: 90,
};
for (let a in student) {
  console.log("Marks of " + a + " are " + student[a]);
}
