let arr = [1, 2, 3, 4, 5];
let ar = arr.reduce((h1, h2) => {
  return h1 + h2;
});
console.log(ar);

//the reduce method execute a reducer function for array element
// the reduce() method returns a single value the function accumulated result.
